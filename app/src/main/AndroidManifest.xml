<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mvvm_jetpack_masterclass">

    <uses-feature android:name="android.hardware.camera.any" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".unittesting.MainActivity"></activity>
        <activity android:name=".camerax.PreviewActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".workmanager.HomeActivity" />
        <activity android:name=".retrofit.DisplayActivity" />
        <activity android:name=".room.MainActivity" />
        <activity android:name=".coroutines.CoroutineThreeActivity" />
        <activity android:name=".coroutines.CoroutineTwoActivity" />
        <activity android:name=".coroutines.ParallelDecompositionActivity" />
        <activity android:name=".coroutines.CoroutineOneActivity" />
        <activity android:name=".TwoWayBindingActivity" />
        <activity android:name=".ViewModelActivity" />
        <activity android:name=".DataBinding" />
    </application>

</manifest>